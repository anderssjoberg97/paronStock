const initialDashboardState = {};

/**
 * Handles actions on the dashboard page
 */
export default function dashboardReducer(state = initialDashboardState, action){
    switch(action.type){
        default: {
            break;
        }
    }
    return state;
}
